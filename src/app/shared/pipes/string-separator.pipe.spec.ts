import { StringSeparatorPipe } from './string-separator.pipe';

describe('StringSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new StringSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
